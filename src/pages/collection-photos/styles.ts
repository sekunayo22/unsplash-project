import { css } from "@emotion/css";

const collectionPhotosHeader = css({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
  marginTop: "50px",
});
const collectionPhotosContainer = css({
  margin: "50px auto",
  width: "95vw",
});

export const styles = {
  collectionPhotosContainer,
  collectionPhotosHeader,
};
